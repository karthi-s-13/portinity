\documentclass[letterpaper,10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{geometry}

% Tight Page Geometry Margins for Single-Page Layout
\geometry{left=0.4in, top=0.4in, right=0.4in, bottom=0.4in}

\pagestyle{empty}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% Section Heading Formatting (Compact & Clean)
\titleformat{\section}{
  \vspace{-8pt}\scshape\raggedright\large\bfseries
}{}{0em}{}[\color{black}\hrule \vspace{-4pt}]

\begin{document}

% --- HEADER ---
\begin{center}
    {\Huge \scshape \textbf{ {{ name | latex }} }} \\ \vspace{2pt}
    \small {% if phone %}{{ phone | latex }} $\cdot$ {% endif %}\href{mailto: {{ email }} }{\underline{ {{ email | latex }} }}
    {% if linkedin %} $\cdot$ \href{ {{ linkedin }} }{\underline{ {{ linkedin | latex }} }}{% endif %}
    {% if github %} $\cdot$ \href{ {{ github }} }{\underline{ {{ github | latex }} }}{% endif %}
\end{center}

\vspace{-12pt}

% --- EDUCATION ---
{% if education %}
\section{Education}
  \begin{itemize}[leftmargin=0.15in, label={}, itemsep=1pt, topsep=2pt]
    {% for edu in education %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (edu.institution if edu is mapping else edu) | latex }} } & {{ (edu.location if edu is mapping else '') | latex }} \\
        {{ (edu.degree if edu is mapping else '') | latex }} & {{ (edu.dates if edu is mapping else '') | latex }} \\
      \end{tabular*}
    {% endfor %}
  \end{itemize}
{% endif %}

% --- TECHNICAL SKILLS ---
{% if skills %}
\section{Technical Skills}
\begin{itemize}[leftmargin=0.15in, label={}, itemsep=1pt, topsep=2pt]
  \small{
    {% if skills is mapping %}
      {% for category, items in skills.items() %}
      \item \textbf{ {{ category | latex }}: } {{ items | latex }}
      {% endfor %}
    {% elif skills is iterable and skills is not string %}
      \item \textbf{Core Skills:} {{ skills | latex }}
    {% else %}
      \item {{ skills | latex }}
    {% endif %}
  }
\end{itemize}
{% endif %}

% --- WORK EXPERIENCE ---
{% if experience %}
\section{Experience}
  \begin{itemize}[leftmargin=0.15in, label={}, itemsep=4pt, topsep=2pt]
    {% for exp in experience %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (exp.role if exp is mapping else exp) | latex }} } --- \textit{ {{ (exp.company if exp is mapping else '') | latex }} } & {{ (exp.dates if exp is mapping else '') | latex }} \\
      \end{tabular*}\vspace{-6pt}
      \begin{itemize}[leftmargin=0.2in, label={$\bullet$}, itemsep=1pt, topsep=2pt]
        {% if exp is mapping and exp.bullets %}
          {% if exp.bullets is string %}
            \item {{ exp.bullets | latex }}
          {% elif exp.bullets is iterable %}
            {% for bullet in exp.bullets %}
            \item {{ bullet | latex }}
            {% endfor %}
          {% endif %}
        {% endif %}
      \end{itemize}
    {% endfor %}
  \end{itemize}
{% endif %}

% --- PROJECTS ---
{% if projects %}
\section{Projects}
  \begin{itemize}[leftmargin=0.15in, label={}, itemsep=4pt, topsep=2pt]
    {% for proj in projects %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (proj.title if proj is mapping else proj) | latex }} } $|$ \textit{\small{ {{ (proj.tech_stack if proj is mapping else '') | latex }} }} & {{ (proj.dates if proj is mapping else '') | latex }} \\
      \end{tabular*}\vspace{-6pt}
      \begin{itemize}[leftmargin=0.2in, label={$\bullet$}, itemsep=1pt, topsep=2pt]
        {% if proj is mapping and proj.bullets %}
          {% if proj.bullets is string %}
            \item {{ proj.bullets | latex }}
          {% elif proj.bullets is iterable %}
            {% for bullet in proj.bullets %}
            \item {{ bullet | latex }}
            {% endfor %}
          {% endif %}
        {% endif %}
      \end{itemize}
    {% endfor %}
  \end{itemize}
{% endif %}

% --- CERTIFICATIONS & ACHIEVEMENTS ---
{% if certifications or achievements %}
\section{Certifications \& Achievements}
  \begin{itemize}[leftmargin=0.15in, label={$\bullet$}, itemsep=1pt, topsep=2pt]
    {% if certifications %}
      {% for cert in certifications %}
      \item {% if cert is mapping %}\textbf{ {{ (cert.title or cert.name or 'Certification') | latex }}: } {{ (cert.issuer or cert.issuing_org or '') | latex }}{% else %}{{ cert | latex }}{% endif %}
      {% endfor %}
    {% endif %}
    {% if achievements %}
      {% for ach in achievements %}
      \item {% if ach is mapping %}\textbf{ {{ (ach.title or 'Achievement') | latex }}: } {{ (ach.description or ach.details or '') | latex }}{% else %}{{ ach | latex }}{% endif %}
      {% endfor %}
    {% endif %}
  \end{itemize}
{% endif %}

\end{document}
