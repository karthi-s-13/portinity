\documentclass[letterpaper,10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{geometry}
\usepackage{xcolor}

% Clean, Professional Page Margins
\geometry{left=0.5in, top=0.5in, right=0.5in, bottom=0.5in}

% Modern Sans-Serif Typography (Helvetica-style)
\usepackage[scaled=0.92]{helvet}
\renewcommand{\familydefault}{\sfdefault}

\pagestyle{empty}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% --- COLOR DEFINITIONS ---
\definecolor{primarytext}{HTML}{1E293B} % Deep Charcoal
\definecolor{lightgraytext}{HTML}{475569} % Slate Gray for subtitles
\definecolor{accentblue}{HTML}{1A365D} % Deep Modern Navy Blue

% --- SECTION HEADING FORMATTING ---
\titleformat{\section}{
  \vspace{-6pt}\scshape\raggedright\large\bfseries\color{accentblue}
}{}{0em}{}[\vspace{1.5pt}\color{accentblue}\titlerule[1pt]\vspace{-3pt}]
\titlespacing*{\section}{0pt}{10pt}{5pt}

\begin{document}
\color{primarytext}

% ================= HEADER =================
\begin{center}
    {\Huge \scshape \textbf{\color{accentblue}{ {{ name | latex }} }}} \\[3pt]
    \small \color{lightgraytext}
    {% if location %}{{ location | latex }} \ \textbullet \ {% endif %}
    {% if phone %}{{ phone | latex }} \ \textbullet \ {% endif %}
    \href{mailto:{{ email }}}{\color{primarytext}\underline{ {{ email | latex }} }}
    {% if linkedin %} \ \textbullet \ \href{ {{ linkedin }} }{\color{primarytext}\underline{ {{ linkedin | latex }} }}{% endif %}
    {% if github %} \ \textbullet \ \href{ {{ github }} }{\color{primarytext}\underline{ {{ github | latex }} }}{% endif %}
\end{center}

\vspace{-6pt}

% ================= EDUCATION =================
{% if education %}
\section{Education}
  \begin{itemize}[leftmargin=0in, label={}, itemsep=4pt, topsep=2pt]
    {% for edu in education %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (edu.institution if edu is mapping else edu) | latex }} } & \small\color{lightgraytext}{{ (edu.location if edu is mapping else '') | latex }} \\
        \textit{\small\color{lightgraytext}{{ (edu.degree if edu is mapping else '') | latex }}} & \small\color{lightgraytext}{{ (edu.dates if edu is mapping else '') | latex }} \\
      \end{tabular*}
      {% if edu is mapping and edu.marks %}
        \vspace{1pt}\\\small\color{lightgraytext}{{ edu.marks | latex }}
      {% endif %}
    {% endfor %}
  \end{itemize}
{% endif %}

% ================= TECHNICAL SKILLS =================
{% if skills %}
\section{Technical Skills}
\begin{itemize}[leftmargin=0.15in, label={}, itemsep=2pt, topsep=2pt]
  \small{
    {% if skills is mapping %}
      {% for category, items in skills.items() %}
        {% if items %}
        \item \textbf{ {{ category | latex }}:} {{ items | latex }}
        {% endif %}
      {% endfor %}
    {% elif skills is iterable and skills is not string %}
      \item \textbf{Core Skills:} {{ skills | latex }}
    {% else %}
      \item {{ skills | latex }}
    {% endif %}
  }
\end{itemize}
{% endif %}

% ================= WORK EXPERIENCE =================
{% if experience %}
\section{Experience}
  \begin{itemize}[leftmargin=0in, label={}, itemsep=6pt, topsep=2pt]
    {% for exp in experience %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (exp.role if exp is mapping else exp) | latex }} } \ --- \ \textit{\color{lightgraytext}{{ (exp.company if exp is mapping else '') | latex }}} & \small\color{lightgraytext}{{ (exp.dates if exp is mapping else '') | latex }} \\
      \end{tabular*}\vspace{-4pt}
      \begin{itemize}[leftmargin=0.2in, label={$\cdot$}, itemsep=1.5pt, topsep=2pt]
        {% if exp is mapping and exp.bullets %}
          {% if exp.bullets is string %}
            \item \small {{ exp.bullets | latex }}
          {% elif exp.bullets is iterable %}
            {% for bullet in exp.bullets %}
            \item \small {{ bullet | latex }}
            {% endfor %}
          {% endif %}
        {% endif %}
      \end{itemize}
    {% endfor %}
  \end{itemize}
{% endif %}

% ================= PROJECTS =================
{% if projects %}
\section{Projects}
  \begin{itemize}[leftmargin=0in, label={}, itemsep=6pt, topsep=2pt]
    {% for proj in projects %}
    \item
      \begin{tabular*}{\textwidth}{l@{\extracolsep{\fill}}r}
        \textbf{ {{ (proj.title if proj is mapping else proj) | latex }} } {% if proj is mapping and proj.tech_stack %}\ \color{lightgraytext}{$|$}\ \textit{\small\color{lightgraytext}{ {{ proj.tech_stack | latex }} }}{% endif %} & \small\color{lightgraytext}{{ (proj.dates if proj is mapping else '') | latex }} \\
      \end{tabular*}\vspace{-4pt}
      \begin{itemize}[leftmargin=0.2in, label={$\cdot$}, itemsep=1.5pt, topsep=2pt]
        {% if proj is mapping and proj.bullets %}
          {% if proj.bullets is string %}
            \item \small {{ proj.bullets | latex }}
          {% elif proj.bullets is iterable %}
            {% for bullet in proj.bullets %}
            \item \small {{ bullet | latex }}
            {% endfor %}
          {% endif %}
        {% endif %}
      \end{itemize}
    {% endfor %}
  \end{itemize}
{% endif %}

% ================= CERTIFICATIONS & ACHIEVEMENTS =================
{% if certifications or achievements %}
\section{Certifications \& Achievements}
  \begin{itemize}[leftmargin=0.15in, label={$\cdot$}, itemsep=2pt, topsep=2pt]
    {% if certifications %}
      {% for cert in certifications %}
      \item \small {% if cert is mapping %}\textbf{ {{ (cert.title or cert.name or 'Certification') | latex }}:} {{ (cert.issuer or cert.issuing_org or '') | latex }}{% else %}{{ cert | latex }}{% endif %}
      {% endfor %}
    {% endif %}
    {% if achievements %}
      {% for ach in achievements %}
      \item \small {% if ach is mapping %}\textbf{ {{ (ach.title or 'Achievement') | latex }}:} {{ (ach.description or ach.details or '') | latex }}{% else %}{{ ach | latex }}{% endif %}
      {% endfor %}
    {% endif %}
  \end{itemize}
{% endif %}

\end{document}
